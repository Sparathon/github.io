body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: url('your-animated-background.gif') no-repeat center center fixed; /* Replace with your actual GIF URL */
    background-size: cover;
    color: #333;
}

.container {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    background-color: rgba(255, 255, 255, 0.8); /* White with slight transparency */
}

.header {
    background: url('iyadmain.png') no-repeat center center;
    background-size: cover;
    color: #fff; /* White text to contrast the image */
    padding: 50px;
    text-align: center;
}

.menu {
    display: flex;
    justify-content: center;
    background-color: #333; /* Black background for the menu */
}

.menu a {
    color: #fff; /* White text */
    padding: 14px 20px;
    text-decoration: none;
    text-align: center;
    font-weight: bold;
}

.menu a:hover {
    background-color: #fdd835; /* Yellow on hover */
    color: #000; /* Black text on hover */
}

.main-content {
    flex: 1;
    padding: 20px;
    text-align: center;
}

.subscribe-section {
    margin-top: 20px;
}

.footer {
    background-color: #fdd835; /* Bright yellow */
    color: #000; /* Black text */
    text-align: center;
    padding: 10px;
}
